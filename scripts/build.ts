/**
 * Placeholder build orchestrator.
 *
 * `npm run build` currently invokes esbuild directly from package.json. This
 * file exists so the eventual multi-step build (assets + esbuild + nro) has
 * a home.
 */
export {};
