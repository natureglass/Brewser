/**
 * Placeholder: copy built artifacts into the local Citron SD-card folder for
 * fast iteration. The player has a similar workflow in its docs/packaging
 * notes; this script will mirror it once the browser builds an NRO.
 */
export {};
